<h1>Потребление холодной воды</h1>

<table class="table table-hover">
  <thead>
    <tr>
      <th>Id</th>
      <th>First Name</th>
      <th>Second Name</th>
      <th>Max Cold</th>
    </tr>
  </thead>

  <tbody>
    <% @users_sorted_3_greater_cold.each do |user| %>
      <tr>
        <td><%= user.id %></td>
        <td><%= user.first_name %></td>
        <td><%= user.last_name %></td>
        <td><%= user.max_cold %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to 'Back', show_statistics_path %>